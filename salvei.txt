<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dra. Nicoly Pimentel | Odontologia Especializada</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-rose: #f4dada;
            --dark-rose: #d4a5a5;
            --accent-rose: #b07d7d;
            --light-bg: #fffafa;
            --white: #ffffff;
            --text: #5a4a4a;
            --fb-bg: #f0f2f5;
            --fb-blue: #1877f2;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--white);
            color: var(--text);
            line-height: 1.6;
            font-family: 'Montserrat', sans-serif;
            font-weight: 300;
        }

        h1, h2, h3 {
            font-family: 'Cormorant Garamond', serif;
            font-weight: 600;
            color: var(--accent-rose);
        }

        header {
            background: rgba(255, 255, 255, 0.95);
            padding: 1.2rem 8%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 15px rgba(212, 165, 165, 0.1);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo {
            font-size: 1.5rem;
            color: var(--accent-rose);
            font-family: 'Cormorant Garamond', serif;
            font-weight: 700;
        }

        .logo span { color: var(--dark-rose); }

        nav a {
            text-decoration: none;
            color: var(--text);
            margin-left: 25px;
            font-size: 0.85rem;
            font-weight: 500;
            letter-spacing: 1px;
            text-transform: uppercase;
            transition: 0.3s;
        }

        nav a:hover { color: var(--dark-rose); }

        .hero {
            padding: 120px 8%;
            background: linear-gradient(rgba(255, 255, 255, 0.6), rgba(255, 255, 255, 0.2)), 
                        url('https://images.unsplash.com/photo-1629909613654-28e377c37b09?auto=format&fit=crop&q=80&w=2070') center/cover no-repeat;
            text-align: center;
            min-height: 80vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .hero h1 { font-size: 3.5rem; margin-bottom: 1.5rem; max-width: 900px; line-height: 1.1; }
        .hero p { font-size: 1.2rem; max-width: 600px; margin-bottom: 2.5rem; color: #4a4a4a; font-weight: 500; }

        .btn {
            background: var(--dark-rose);
            color: white;
            padding: 14px 40px;
            border-radius: 2px;
            text-decoration: none;
            font-weight: 500;
            font-size: 0.9rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: 0.4s;
            display: inline-block;
            border: 1px solid var(--dark-rose);
            cursor: pointer;
        }

        .btn:hover { background: white; color: var(--dark-rose); transform: translateY(-3px); }

        /* --- SEÇÃO DE COMENTÁRIOS E FORMULÁRIO --- */
        .fb-comments-section {
            background-color: var(--fb-bg);
            padding: 60px 8%;
        }

        .fb-flex-layout {
            display: flex;
            gap: 30px;
            max-width: 1100px;
            margin: 0 auto;
            align-items: flex-start;
        }

        /* Lado Esquerdo: Formulário */
        .fb-form-card {
            flex: 1;
            background: white;
            padding: 25px;
            border-radius: 8px;
            box-shadow: 0 1px 2px rgba(0,0,0,0.1);
        }

        .fb-form-card h3 { font-size: 1.4rem; margin-bottom: 20px; color: #1c1e21; }

        .fb-form-group { margin-bottom: 15px; }
        .fb-form-group label { display: block; font-size: 0.85rem; font-weight: 600; margin-bottom: 5px; color: #65676b; }
        
        .fb-input {
            width: 100%;
            padding: 10px;
            border: 1px solid #dddfe2;
            border-radius: 6px;
            font-family: inherit;
            font-size: 0.9rem;
            outline: none;
        }
        .fb-input:focus { border-color: var(--fb-blue); }

        .fb-textarea { height: 100px; resize: none; }

        /* Lado Direito: Feed */
        .fb-container {
            flex: 1.5;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 1px 2px rgba(0,0,0,0.1);
            font-family: Segoe UI, Helvetica, Arial, sans-serif;
            max-height: 600px;
            overflow-y: auto;
        }

        .fb-header {
            font-size: 16px;
            font-weight: 600;
            color: #1c1e21;
            border-bottom: 1px solid #ebedf0;
            padding-bottom: 10px;
            margin-bottom: 15px;
        }

        .comment-item { display: flex; gap: 8px; margin-bottom: 5px; }
        .user-avatar { width: 36px; height: 36px; border-radius: 50%; object-fit: cover; }
        .comment-bubble { background-color: #f0f2f5; padding: 8px 12px; border-radius: 18px; max-width: 90%; }
        .user-name { font-size: 13px; font-weight: 600; color: #050505; display: block; text-decoration: none; }
        .comment-text { font-size: 15px; color: #050505; line-height: 1.3; }
        .comment-actions { margin-left: 44px; font-size: 12px; color: #65676b; font-weight: 600; margin-bottom: 15px; }
        .comment-actions span { cursor: pointer; margin-right: 8px; }

        /* Estilos Restantes */
        .services { padding: 100px 8%; background: var(--white); }
        .services h2 { text-align: center; font-size: 2.5rem; margin-bottom: 50px; }
        .services-container { display: flex; flex-wrap: wrap; gap: 40px; align-items: center; }
        .services-image img { width: 100%; border-radius: 100px 100px 0 0; box-shadow: 20px 20px 0px var(--primary-rose); object-fit: cover; height: 450px; }
        .grid { flex: 1; display: grid; grid-template-columns: 1fr; gap: 1.5rem; min-width: 300px; }
        .card { padding: 30px; background: var(--light-bg); border-left: 4px solid var(--primary-rose); }
        .info-box { background: var(--primary-rose); padding: 80px 8%; text-align: center; }
        .info-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 3rem; }
        .contact-section { padding: 80px 8%; display: flex; flex-wrap: wrap; align-items: center; gap: 50px; }
        .contact-img img { width: 100%; border-radius: 20px; }
        footer { background: #4a3f3f; color: var(--primary-rose); padding: 50px 8%; text-align: center; font-size: 0.8rem; }

        @media (max-width: 900px) {
            .fb-flex-layout { flex-direction: column; }
            .fb-form-card, .fb-container { width: 100%; }
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">Dra. <span>Nicoly Pimentel</span></div>
        <nav>
            <a href="#home">Início</a>
            <a href="#servicos">Especialidades</a>
            <a href="#carreira">A Carreira</a>
            <a href="#contato">Contato</a>
        </nav>
    </header>

    <section class="hero" id="home">
        <h1>Sorrisos que transformam vidas.</h1>
        <p>A união entre a delicadeza do cuidado e a precisão da odontologia moderna.</p>
        <a href="#contato" class="btn">Agendar Avaliação</a>
    </section>

    <section class="services" id="servicos">
        <h2>Nossas Especialidades</h2>
        <div class="services-container">
            <div class="services-image" style="flex:1; min-width:300px;">
                <img src="https://images.unsplash.com/photo-1606811841689-23dfddce3e95?auto=format&fit=crop&q=80&w=1000" alt="Odontologia">
            </div>
            <div class="grid">
                <div class="card"><h3>Clareamento Profissional</h3><p>Tecnologia avançada para devolver o brilho natural.</p></div>
                <div class="card"><h3>Lentes de Porcelana</h3><p>A perfeição estética desenhada exclusivamente.</p></div>
                <div class="card"><h3>Prevenção e Saúde</h3><p>Check-ups regulares para manter sua saúde em dia.</p></div>
            </div>
        </div>
    </section>

    <section class="info-box" id="carreira">
        <div class="info-grid">
            <div class="info-item"><h3>R$ 3k e 20k</h3><p>Média Salarial Variável</p></div>
            <div class="info-item"><h3>700 e 800</h3><p>Notas de Corte ENEM</p></div>
            <div class="info-item"><h3>5 Anos</h3><p>Formação Acadêmica</p></div>
        </div>
    </section>

    <section class="contact-section" id="contato">
        <div class="contact-img" style="flex:1; min-width:300px;">
            <img src="https://images.unsplash.com/photo-1588776814546-1ffcf47267a5?auto=format&fit=crop&q=80&w=1000" alt="Agendamento">
        </div>
        <div class="contact-content" style="flex:1;">
            <h2>Agende seu momento</h2>
            <p>Rua da Odontologia, 123 • Sala 405</p>
            <p style="margin-bottom: 20px;">Segunda a Sexta: 09h às 18h</p>
            <a href="#" class="btn" style="background: #25d366; border-color: #25d366;">WhatsApp</a>
        </div>
    </section>

    <section class="fb-comments-section">
        <div class="fb-flex-layout">
            
            <div class="fb-form-card">
                <h3>Deixe um Comentário</h3>
                <div class="fb-form-group">
                    <label>Seu Nome</label>
                    <input type="text" id="userName" class="fb-input" placeholder="Ex: Maria Souza">
                </div>
                <div class="fb-form-group">
                    <label>Link da sua Foto (URL)</label>
                    <input type="text" id="userPhoto" class="fb-input" placeholder="https://linkdaimagem.com/foto.jpg">
                </div>
                <div class="fb-form-group">
                    <label>Mensagem</label>
                    <textarea id="userMsg" class="fb-input fb-textarea" placeholder="O que você achou do atendimento?"></textarea>
                </div>
                <button onclick="adicionarComentario()" class="btn" style="width: 100%; background: var(--fb-blue); border-color: var(--fb-blue);">Publicar no Facebook</button>
            </div>

            <div class="fb-container">
                <div class="fb-header">Comentários Recentes</div>
                
                <div id="feedComentarios">
                    <div class="comment-item">
                        <img src="https://i.pravatar.cc/150?u=mari" class="user-avatar">
                        <div class="comment-bubble">
                            <span class="user-name">Juliana Mendes</span>
                            <p class="comment-text">Simplesmente encantada com o atendimento! 😍🦷</p>
                        </div>
                    </div>
                    <div class="comment-actions"><span>Curtir</span> · <span>Responder</span> · 2 sem</div>

                    <div class="comment-item">
                        <img src="https://i.pravatar.cc/150?u=pedro" class="user-avatar">
                        <div class="comment-bubble">
                            <span class="user-name">Carlos Eduardo</span>
                            <p class="comment-text">Trabalho impecável nas lentes de porcelana!</p>
                        </div>
                    </div>
                    <div class="comment-actions"><span>Curtir</span> · <span>Responder</span> · 1 d</div>
                </div>
            </div>

        </div>
    </section>

    <footer>
        <p>&copy; 2026 DRA. NICOLY PIMENTEL • TODOS OS DIREITOS RESERVADOS</p>
    </footer>

    <script>
        function adicionarComentario() {
            const nome = document.getElementById('userName').value;
            const msg = document.getElementById('userMsg').value;
            let foto = document.getElementById('userPhoto').value;

            if(nome === "" || msg === "") {
                alert("Por favor, preencha o nome e a mensagem.");
                return;
            }

            // Foto padrão caso o link esteja vazio
            if(foto === "") { foto = "https://via.placeholder.com/150"; }

            const feed = document.getElementById('feedComentarios');

            // Criar a estrutura do novo comentário
            const novoComentario = document.createElement('div');
            novoComentario.innerHTML = `
                <div class="comment-item" style="margin-top: 15px;">
                    <img src="${foto}" class="user-avatar" onerror="this.src='https://via.placeholder.com/150'">
                    <div class="comment-bubble">
                        <span class="user-name">${nome}</span>
                        <p class="comment-text">${msg}</p>
                    </div>
                </div>
                <div class="comment-actions"><span>Curtir</span> · <span>Responder</span> · Agora</div>
            `;

            // Adicionar no topo do feed
            feed.prepend(novoComentario);

            // Limpar campos
            document.getElementById('userName').value = "";
            document.getElementById('userMsg').value = "";
            document.getElementById('userPhoto').value = "";
        }
    </script>

</body>
</html>